name: DVD Cutover - Read-only Smoke

on:
  pull_request:
    branches: [main]

jobs:
  schedule-diagnostic-readonly:
    name: DVD 38C - Mongo read-only smoke
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      mongo:
        image: mongo:6
        ports:
          - 27017:27017
    env:
      MONGO_URL: mongodb://localhost:27017
      DB_NAME: sigesc_dvd_cutover_ci

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install minimal dependencies
        run: pip install motor python-dotenv

      - name: Wait for MongoDB
        run: |
          for i in $(seq 1 30); do
            if python -c "from pymongo import MongoClient; MongoClient('$MONGO_URL', serverSelectionTimeoutMS=1000).admin.command('ping')" 2>/dev/null; then
              echo "MongoDB pronto"; exit 0
            fi
            sleep 1
          done
          echo "MongoDB não respondeu"; exit 1

      - name: Run 38C read-only diagnostic
        run: |
          python scripts/audit_dvd_schedule_blockers.py \
            --academic-year 2026 \
            --json /tmp/dvd-schedule-blockers-ci.json
          test -s /tmp/dvd-schedule-blockers-ci.json

      - name: Assert read-only contract
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          report = json.loads(Path('/tmp/dvd-schedule-blockers-ci.json').read_text())
          assert report['meta']['mode'] == 'READ_ONLY_SCHEDULE_DIAGNOSTIC'
          assert report['meta']['academic_year'] == 2026
          assert report['meta']['mutates_database'] is False
          print('DVD 38C read-only smoke PASS')
          PY
