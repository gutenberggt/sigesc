name: Phase 38F PDF apply

on:
  push:
    branches: [agent/dvd-cutover-audit]

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/dvd-cutover-audit
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Apply guarded PDF patch
        run: python tools/phase38f_pdf_apply.py

      - name: Validate backend syntax and diff
        run: |
          python -m compileall -q backend/routers/learning_objects.py
          git diff --check

      - name: Remove temporary applicator
        run: rm tools/phase38f_pdf_apply.py .github/workflows/phase38f-pdf-apply.yml

      - name: Commit final PDF patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(dvd): vincular PDF de conteúdos ao assignment_id"
          git push origin HEAD:agent/dvd-cutover-audit
