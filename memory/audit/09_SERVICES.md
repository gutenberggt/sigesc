# ENTREGA 09 — Services

⏳ **PENDENTE — Onda 2.**

Escopo: os **~44 services backend** classificados por domínio (risco/IA, PDF/render,
diário/snapshot, currículo, bolsa família, relatórios, calendário, etc.) —
finalidade e duplicações. Atenção especial à possível unificação dos motores de
risco (`academic_risk_engine`, `attendance_risk_engine`, `overall_risk_engine`,
`diagnostic_engine`) e às bridges legadas (`legacy_schedule_bridge`,
`legacy_content_bridge`).
