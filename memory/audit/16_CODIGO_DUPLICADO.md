# ENTREGA 16 — Código Duplicado

⏳ **PENDENTE — Onda 2.** (Candidatos já sinalizados na Onda 1.)

Candidatos prioritários a consolidação:
- 🔴 Grade horária: `class_schedules` × `teacher_assignments` × `teacher_allocations`.
- 🔴 Vínculo aluno↔turma: `enrollments` × `students.class_id` × `class_students`.
- 🟡 Motores de risco: academic/attendance/overall/diagnostic (scoring sobreposto).
- 🟡 Snapshots: `snapshots` × `diary_snapshots` × `student_snapshots` × `ai_analysis_snapshots`.
- 🟡 Jobs de render: `render_jobs` × `document_render_jobs`.
- 🟡 Canonicalização de série / lógica de status espalhada.
Classificar por prioridade e estimar esforço na Onda 2.
