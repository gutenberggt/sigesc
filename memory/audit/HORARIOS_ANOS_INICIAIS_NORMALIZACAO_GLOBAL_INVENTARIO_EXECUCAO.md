# Gate de execução

Este PR contém somente inventário read-only. A futura escrita em `class_schedules` dependerá de inventário de produção, backup, dry-run controlado e autorização humana explícita separada.